<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
    <title><%= text.giris_baslik %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body { font-family: 'Segoe UI', sans-serif; background: #f0f2f5; display: flex; justify-content: center; align-items: center; height: 100vh; flex-direction: column; margin: 0; }
        .dil-btn { position: absolute; top: 20px; right: 20px; background: #fff; color: #333; padding: 8px 15px; text-decoration: none; border-radius: 20px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); font-weight: bold; }
        
        .login-box { 
            background: white; padding: 40px; border-radius: 12px; 
            width: 350px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); 
        }
        
        h2 { text-align: center; color: #333; margin-top: 0; }
        
        input { 
            width: 100%; padding: 12px; margin: 10px 0; 
            border: 1px solid #ddd; border-radius: 6px; box-sizing: border-box; 
        }
        
        button { 
            width: 100%; padding: 12px; background: #333; color: white; 
            font-weight: bold; cursor: pointer; border: none; border-radius: 6px; margin-top: 10px; transition: 0.3s;
        }
        button:hover { background: #555; }
        
        /* STANDART HATA */
        .hata { color: red; font-weight: bold; margin-bottom: 10px; text-align: center; font-size: 0.9rem; }
        
        /* KİLİTLİ HESAP UYARISI */
        .locked-box {
            background-color: #ffebee;
            color: #c62828;
            border: 2px solid #ef5350;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            font-size: 0.95rem;
            line-height: 1.5;
            text-align: center;
        }
        .locked-icon { font-size: 2rem; display: block; margin-bottom: 5px; }
    </style>
</head>
<body>
    <a href="?lang=<%= lang === 'tr' ? 'en' : 'tr' %>" class="dil-btn"><%= text.dil_btn %></a>
    
    <div class="login-box">
        <h2><%= text.giris_baslik %></h2>

        <% if(hata === 'LOCKED') { %>
            <div class="locked-box">
                <span class="locked-icon">🔒</span>
                <b>HESAP KİLİTLENDİ!</b><br>
                5 kez hatalı giriş yapıldı.<br><br>
                Lütfen <b>Yönetici</b> ile iletişime geçiniz.
            </div>
        <% } else if(hata) { %> 
            <div class="hata"><%= hata %></div> 
        <% } %>
        
        <form action="/login" method="POST">
            <label style="font-weight: 500;"><%= text.kadi %></label>
            <input type="text" name="kadi" required>
            
            <label style="font-weight: 500;"><%= text.sifre %></label>
            <input type="password" name="sifre" required>
            
            <button type="submit"><%= text.giris_btn %></button>
        </form>
    </div>
</body>
</html>